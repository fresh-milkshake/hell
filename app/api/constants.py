from datetime import timedelta

ONLY_FOR_INTERNAL_USAGE = "Access denied: This endpoint is accessible only from the manager network."

INVITATION_EXPIRE_DELTA = timedelta(hours=24)
API_KEY_HEADER_NAME = "X-API-KEY"
